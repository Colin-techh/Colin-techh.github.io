function Next({nextPassage}) {
    return(
        <div>
            <button
                className="modern-button"
                onClick={nextPassage}
                >Next</button>
        </div>
    );
}
export default Next;